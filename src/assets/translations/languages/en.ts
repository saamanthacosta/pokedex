import { Translations } from '../type';

const translations: Translations['en'] = {
  'header:name': 'Pokedex',
  'header-filter:placeholder': 'Search...',
  'home:no-pokemons-list': `No pokemons here :(`
};

export default translations;
