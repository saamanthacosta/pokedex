import { Translations } from '../type';

const translations: Translations['pt'] = {
  'header:name': 'Pokédex',
  'header-filter:placeholder': 'Pesquisar...',
  'home:no-pokemons-list': 'Não tem pokemons aqui :('
};

export default translations;
